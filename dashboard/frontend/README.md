# Tryzub Trade — React Dashboard (Vite)

```bash
cd dashboard/frontend
npm install
npm run dev
```

Opens on http://localhost:5173 and talks to the bot API at `VITE_API_URL` (default http://localhost:8080).

```bash
# bot (HTTP for local)
FORCE_HTTPS=false python tryzub_trade_bot.py
```
